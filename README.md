UART communication with the ESP32 evaluation board<br><br>

Send_String.py           - Send a string via USB to ESP32<br>
Read_String_from_UART0.c - Read a string sent via UART0 and print it in the serial monitor

